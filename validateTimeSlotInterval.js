/**
* Validation code for field timeSlotInterval on config
* @param {import("gadget-server").ConfigTimeSlotIntervalFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.timeSlotInterval` to get the fields's data
  // add to the `errors` with `errors.add('timeSlotInterval', "did not pass the validation")`
};